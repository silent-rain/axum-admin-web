[package]
name = "public"
version = "0.1.0"
edition = "2024"

[dependencies]
embed_asset = { path = "../../embed_asset" }

axum_response = { workspace = true }

axum = { workspace = true }
tower-http = { workspace = true, features = ["fs"] }

log = { workspace = true }
serde = { workspace = true, features = ["derive"] }
