[package]
name = "timer"
version = "0.1.0"
edition = "2024"

[dependencies]
scheduler = { path = "../core/scheduler" }

database = { workspace = true }
entity = { workspace = true }


tokio = { workspace = true }
